name: Trying scripts in actions
on: [push]

jobs:
  example-job:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./scripts
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5
      - name: Run a script
        run: ./meripehliscript.sh
